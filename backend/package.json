{
  "name": "koorazone-football-backend",
  "version": "1.0.0",
  "description": "Serves GET /football-stories to the KOORAZONE FOOT mobile app. No external API key or database — just a static curated list. Live scores and standings for the Blogger theme come straight from worldcup26.ir client-side, not through this service.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0"
  }
}
