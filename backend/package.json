{
  "name": "koorazone-football-backend",
  "version": "1.0.0",
  "description": "Polls a football data API on a schedule and caches live scores, standings and top scorers into the koorazone-e59a7 Firestore project, so the KOORAZONE Blogger theme can read them client-side without hitting the football API's rate limit itself.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.5.0",
    "helmet": "^7.1.0"
  }
}
